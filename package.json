{
  "name": "fp-plugins",
  "version": "0.1.0",
  "private": true,
  "description": "Flarepoint plugin hub — a Claude Code marketplace of behavioural plugins built on a shared substrate.",
  "license": "MIT",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "test": "node --test \"shared/fpbase/test/*.test.js\" \"plugins/*/test/*.test.js\""
  }
}
